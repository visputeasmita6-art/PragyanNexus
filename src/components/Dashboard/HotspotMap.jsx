import React from 'react'

const HotspotMap = () => {
  return (
    <div style={{ background: '#0f2136', borderRadius: 12, padding: 12, height: 180, display: 'flex', alignItems: 'center', justifyContent: 'center', border: '1px solid var(--border-subtle)' }}>
      <span style={{ color: 'var(--text-muted)' }}>🗺️ Map view (simulated)</span>
    </div>
  )
}

export default HotspotMap